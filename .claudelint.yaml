strict: true

# Disable rules for marketplace repos (skills, not commands)
rules:
  commands-dir-required:
    enabled: false
  commands-exist:
    enabled: false
  command-naming:
    enabled: false
  command-frontmatter:
    enabled: false
  command-sections:
    enabled: false
  command-name-format:
    enabled: false
